extends ../../../aalayout/layout

block content
	.container.mt-3
		.title
			.row
				.col-9.text-muted.mt-2: h5
					a(href="/") Home
					span &nbsp; | &nbsp;
					a(href="/mgDuts") 采购订单列表
					span &nbsp; | &nbsp;
					span 采购单详情
				.col-3.text-right
			hr
		.info.my-3
			include ./detail/basicInfo
			if dut.status == Conf.status.init.num
				-a;
				include ./compd/add
			else
				include ./detail/billInfo